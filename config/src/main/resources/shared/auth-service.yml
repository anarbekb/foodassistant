spring:
  data:
    mongodb:
      host: auth-mongodb
      username: user
      password: ${MONGODB_PASSWORD}
      database: foodassistant
      port: 27017

server:
  servlet:
    context-path: /uaa
  port: 5000
